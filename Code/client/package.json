{
  "name": "elementice-bulletkam_client",
  "version": "1.0.0",
  "description": "The client which runs on a Raspberry Pi to deliver images to the server via POST request",
  "main": "client.js",
  "scripts": {
    "start": "node client.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "file-system": "^2.2.2",
    "request": "^2.88.0"
  }
}
